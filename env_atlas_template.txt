# MongoDB Atlas Configuration Template
# Copy this content to your .env file and configure your Atlas credentials
# This will significantly improve your MongoDB Atlas performance

# =============================================================================
# ATLAS AUTHENTICATION (Required for Atlas)
# =============================================================================
# Your Atlas username (not your MongoDB Atlas account email)
ATLAS_USERNAME=your_atlas_username

# Your Atlas password (not your MongoDB Atlas account password)
ATLAS_PASSWORD=your_atlas_password

# Your Atlas cluster hostname (e.g., cluster0.xxxxx.mongodb.net)
# Find this in Atlas Dashboard -> Connect -> Connect your application
ATLAS_CLUSTER_HOST=cluster0.xxxxx.mongodb.net

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Database name in your Atlas cluster
DATABASE_NAME=warehouse_management

# Application name for connection tracking in Atlas
ATLAS_APP_NAME=warehouse-management-system

# =============================================================================
# FALLBACK CONFIGURATION (for local development)
# =============================================================================
# If Atlas environment variables are not set, these will be used
# MONGODB_URL=mongodb://localhost:27017

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Security key for JWT tokens
SECRET_KEY=your-very-secure-secret-key-change-this

# Debug mode (set to False in production)
DEBUG_MODE=True

# Environment (development, staging, production)
ENVIRONMENT=development

# Logging level
LOGGING_LEVEL=INFO

# =============================================================================
# CHATBOT CONFIGURATION (Optional)
# =============================================================================
# OpenAI API key for chatbot functionality
OPENAI_API_KEY=your_openai_api_key_here

# LLM model to use
LLM_MODEL=gpt-4o-mini

# LLM temperature (0.0 to 1.0)
LLM_TEMPERATURE=0.3

# Development mode settings
DEV_MODE=True
DEV_USER_ROLE=Manager

# =============================================================================
# CHROMA DB CONFIGURATION (for chatbot knowledge base)
# =============================================================================
CHROMA_PERSIST_DIRECTORY=./chroma_db
CHROMA_COLLECTION_NAME=wms_knowledge
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# =============================================================================
# PERFORMANCE OPTIMIZATION SETTINGS
# =============================================================================
# These are automatically configured by the atlas_optimization module
# But you can override them if needed

# Connection pool settings (automatically optimized)
# MAX_POOL_SIZE=100
# MIN_POOL_SIZE=10

# Timeout settings in milliseconds (automatically optimized for Atlas)
# CONNECTION_TIMEOUT_MS=30000
# SOCKET_TIMEOUT_MS=30000
# SERVER_SELECTION_TIMEOUT_MS=30000

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this content to a .env file in the backend directory
# 2. Replace all placeholder values with your actual Atlas credentials
# 3. Run: python atlas_performance_setup.py
# 4. Start your application
#
# Need help?
# - Atlas credentials: https://docs.atlas.mongodb.com/tutorial/create-mongodb-user-for-cluster/
# - Connection string: https://docs.atlas.mongodb.com/driver-connection/
# - Performance tuning: Check the generated atlas_optimization.py file 